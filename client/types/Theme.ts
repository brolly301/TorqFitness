export type Theme = {
  // Base
  background: string;
  surface: string;
  card: string;
  border: string;

  // Text
  textPrimary: string;
  textSecondary: string;
  textMuted: string;

  // Actions
  primary: string;
  primaryText: string;

  secondary: string;
  secondaryText: string;

  // States
  success: string;
  error: string;
  warning: string;
  disabled: string;

  // Inputs
  inputBackground: string;
  inputBorder: string;
  inputText: string;
  inputPlaceholder: string;

  // UI Details
  hover: string;
  focus: string;
  overlay: string;
  shadow: string;

  // Navigation
  navBorder: string;
};
